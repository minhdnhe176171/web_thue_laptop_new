@model IEnumerable<web_chothue_laptop.Models.Customer>

@{
    ViewData["Title"] = "Quản Lý Customer Blacklist";
    Layout = "_ManagerLayout";
    var searchValue = ViewBag.Search as string ?? "";
    var filterStatusValue = ViewBag.FilterStatus as string ?? "";
    var totalItems = ViewBag.TotalItems as int? ?? 0;
    var currentPage = ViewBag.CurrentPage as int? ?? 1;
    var totalPages = ViewBag.TotalPages as int? ?? 1;
}

<style>
    .blacklist-badge {
        background-color: #dc3545;
        color: white;
        padding: 4px 8px;
        border-radius: 4px;
        font-size: 0.75rem;
        font-weight: bold;
    }

    .table-custom thead th {
        background-color: #0d6efd;
        color: white;
        text-align: center;
        vertical-align: middle;
    }

    .table-custom tbody td {
        text-align: center;
        vertical-align: middle;
    }

    .table-custom tbody tr.table-danger:hover {
        background-color: #f8d7da;
    }
</style>

<h3 class="fw-bold text-primary mb-4">
    <i class="fas fa-users"></i>
    Quản Lý Customer Blacklist (@totalItems)
</h3>

@if (TempData["SuccessMessage"] != null)
{
    <div class="alert alert-success">
        <i class="fas fa-check-circle me-2"></i>@TempData["SuccessMessage"]
    </div>
}
@if (TempData["ErrorMessage"] != null)
{
    <div class="alert alert-danger">
        <i class="fas fa-exclamation-circle me-2"></i>@TempData["ErrorMessage"]
    </div>
}

<!-- FILTER -->
<div class="card shadow-sm mb-3">
    <div class="card-body">
        <form method="get" asp-action="CustomerManagement" id="filterForm">
            <div class="row g-3">
                <div class="col-md-6">
                    <label class="form-label">Tìm kiếm</label>
                    <input type="text"
                           class="form-control"
                           name="search"
                           value="@searchValue"
                           placeholder="Tên, Email, Số điện thoại..." />
                </div>

                <div class="col-md-6">
                    <label class="form-label">Trạng thái</label>
                    <select class="form-select" name="filterStatus" id="filterStatus">
                        <option value="">Tất cả</option>
                        @{
                            var normalSelected = filterStatusValue == "normal";
                            var blacklistSelected = filterStatusValue == "blacklist";
                        }
                        <option value="normal" selected="@(normalSelected ? "selected" : null)">
                            Bình thường
                        </option>
                        <option value="blacklist" selected="@(blacklistSelected ? "selected" : null)">
                            Blacklist
                        </option>
                    </select>
                </div>
            </div>

            @if (!string.IsNullOrEmpty(searchValue)
                        || !string.IsNullOrEmpty(filterStatusValue))
            {
                <div class="mt-3">
                    <a asp-action="CustomerManagement" class="btn btn-secondary btn-sm">
                        <i class="fas fa-times"></i> Xóa bộ lọc
                    </a>
                </div>
            }
        </form>
    </div>
</div>

<!-- TABLE -->
<div class="card shadow-sm">
    <div class="card-body p-0">
        <table class="table table-bordered table-custom mb-0">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Họ tên</th>
                    <th>Email</th>
                    <th>SĐT</th>
                    <th>CMND/CCCD</th>
                    <th>Ngày tạo</th>
                    <th>Trạng thái</th>
                    <th>Hành động</th>
                </tr>
            </thead>
            <tbody>
                @if (Model != null && Model.Any())
                {
                    foreach (var customer in Model)
                    {
                        <tr class="@(customer.BlackList == true ? "table-danger" : "")">
                            <td class="fw-bold">#@customer.Id</td>
                            <td>@customer.LastName @customer.FirstName</td>
                            <td>@customer.Email</td>
                            <td>@(customer.Phone ?? "N/A")</td>
                            <td>@(customer.IdNo ?? "N/A")</td>
                            <td>@customer.CreatedDate?.ToString("dd/MM/yyyy")</td>
                            <td>
                                @if (customer.BlackList == true)
                                {
                                    <span class="blacklist-badge">
                                        <i class="fas fa-ban"></i> BLACKLIST
                                    </span>
                                }
                                else
                                {
                                    <span class="badge bg-success">Bình thường</span>
                                }
                            </td>
                            <td>
                                <a asp-action="RentalHistory"
                                   asp-route-id="@customer.Id"
                                   class="btn btn-primary btn-sm">
                                    <i class="fas fa-history"></i> Lịch sử
                                </a>
                            </td>
                        </tr>
                    }
                }
                else
                {
                    <tr>
                        <td colspan="8" class="text-center text-muted py-4">
                            Không có dữ liệu
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

<!-- PAGINATION -->
@if (totalPages > 1)
{
    <nav class="mt-3">
        <ul class="pagination justify-content-center">
            @for (int i = 1; i <= totalPages; i++)
            {
                <li class="page-item @(currentPage == i ? "active" : "")">
                    <a class="page-link"
                       asp-action="CustomerManagement"
                       asp-route-search="@searchValue"
                       asp-route-filterStatus="@filterStatusValue"
                       asp-route-page="@i">
                        @i
                    </a>
                </li>
            }
        </ul>
    </nav>
}