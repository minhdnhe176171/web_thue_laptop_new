@{
    var currentController = ViewContext.RouteData.Values["controller"]?.ToString();
}

<div class="admin-sidebar">
    <div class="sidebar-header">
        <h5 class="mb-0">Quản trị Admin</h5>
    </div>
    <nav class="sidebar-nav">
        <ul class="nav flex-column">
            <li class="nav-item">
                <a class="nav-link @(currentController == "CreateAccount" ? "active" : "")"
                   asp-controller="CreateAccount" asp-action="Index">
                    ➕ Tạo tài khoản
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link @(currentController == "ManageAccount" ? "active" : "")"
                   asp-controller="ManageAccount" asp-action="Index">
                    🔒 Quản lý tài khoản
                </a>
            </li>
        </ul>
    </nav>
</div>

<style>
    .admin-sidebar {
        background-color: #f8f9fa;
        border-right: 1px solid #dee2e6;
        min-height: calc(100vh - 200px);
        padding: 20px 0;
    }

    .sidebar-header {
        padding: 0 20px 20px 20px;
        border-bottom: 2px solid #dee2e6;
        margin-bottom: 20px;
    }

        .sidebar-header h5 {
            color: #495057;
            font-weight: 600;
        }

    .sidebar-nav .nav-link {
        color: #495057;
        padding: 12px 20px;
        border-left: 3px solid transparent;
        transition: all 0.3s;
        text-decoration: none;
    }

        .sidebar-nav .nav-link:hover {
            background-color: #e9ecef;
            border-left-color: #0d6efd;
            color: #0d6efd;
        }

        .sidebar-nav .nav-link.active {
            background-color: #e7f1ff;
            border-left-color: #0d6efd;
            color: #0d6efd;
            font-weight: 600;
        }
</style>