@{
    var currentAction = ViewContext.RouteData.Values["action"]?.ToString()?.ToLower() ?? "student";
    var currentController = ViewContext.RouteData.Values["controller"]?.ToString();
    var isCreateAccount = currentController?.ToLower() == "createaccount";
}

<div class="manage-account-sidebar">
    <div class="sidebar-header">
        <h5 class="mb-0">Quản lý tài khoản</h5>
    </div>
    <nav class="sidebar-nav">
        <ul class="nav flex-column">
            <li class="nav-item">
                <a class="nav-link @(isCreateAccount ? "active" : "")" 
                   asp-controller="CreateAccount" asp-action="Index">
                    ➕ Tạo tài khoản
                </a>
            </li>
            <li class="nav-item" style="margin-top: 10px; padding-top: 10px; border-top: 1px solid #dee2e6;">
                <span style="padding: 8px 20px; font-size: 0.75rem; color: #6c757d; font-weight: 600; text-transform: uppercase;">Danh sách theo vai trò</span>
            </li>
            <li class="nav-item">
                <a class="nav-link @(currentAction == "student" ? "active" : "")" 
                   asp-controller="ManageAccount" asp-action="Student">
                    👨‍🎓 Student
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link @(currentAction == "customer" ? "active" : "")" 
                   asp-controller="ManageAccount" asp-action="Customer">
                    👤 Customer
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link @(currentAction == "staff" ? "active" : "")" 
                   asp-controller="ManageAccount" asp-action="Staff">
                    👔 Staff
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link @(currentAction == "manager" ? "active" : "")" 
                   asp-controller="ManageAccount" asp-action="Manager">
                    👨‍💼 Manager
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link @(currentAction == "technical" ? "active" : "")" 
                   asp-controller="ManageAccount" asp-action="Technical">
                    🔧 Technical
                </a>
            </li>
        </ul>
    </nav>
</div>

<style>
    .manage-account-sidebar {
        background-color: #f8f9fa;
        border-right: 1px solid #dee2e6;
        min-height: calc(100vh - 200px);
        padding: 20px 0;
    }

    .sidebar-header {
        padding: 0 20px 20px 20px;
        border-bottom: 2px solid #dee2e6;
        margin-bottom: 20px;
    }

    .sidebar-header h5 {
        color: #495057;
        font-weight: 600;
    }

    .sidebar-nav .nav-link {
        color: #495057;
        padding: 12px 20px;
        border-left: 3px solid transparent;
        transition: all 0.3s;
        text-decoration: none;
        display: block;
    }

    .sidebar-nav .nav-link:hover {
        background-color: #e9ecef;
        border-left-color: #0d6efd;
        color: #0d6efd;
    }

    .sidebar-nav .nav-link.active {
        background-color: #e7f1ff;
        border-left-color: #0d6efd;
        color: #0d6efd;
        font-weight: 600;
    }
</style>

